let texto: string = "oi";
let num: number = 10;
let ativo: boolean = true;

let lista: number[] = [1, 2, 3];

let coord: [number, number] = [-25, -49];

enum Perfil {
  ADMIN,
  USER,
}

console.log(texto, num, ativo, lista, coord, Perfil.ADMIN);